{
  "name": "solar-wing-fixture-planner",
  "version": "1.0.0",
  "description": "Satellite solar wing support-pad fixture planner (four rails, CG deviation robustness)",
  "type": "module",
  "private": true,
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "test": "node --test",
    "smoke": "node scripts/smoke.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
